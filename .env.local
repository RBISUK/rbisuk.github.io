# --- SMTP (email receipts) ---
SMTP_HOST=smtp.yourhost.com
SMTP_PORT=587               # 465 if SSL
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM="RBIS UK <no-reply@yourdomain.com>"

# --- AWS S3 (evidence uploads) ---
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET
AWS_REGION=eu-west-2
S3_BUCKET=your-bucket-name
# Optional custom CDN/base (else we'll use bucket.s3.region.amazonaws.com)
# S3_PUBLIC_URL=https://cdn.yourdomain.com
# If your bucket allows it, you may set ACL public read on put:
# S3_ACL_PUBLIC=1
