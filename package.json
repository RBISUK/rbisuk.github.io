{
  "name": "rbisuk.github.io",
  "version": "1.0.0",
  "description": "Push to `main` → auto-deploys to **GitHub Pages** via Actions.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "echo \"no tests\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
